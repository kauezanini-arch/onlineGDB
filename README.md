#include <stdio.h>

int main()
{
    int a,b,c,d;
    
    scanf("%d %d %d", &a, &b, &c);
    
    printf("Digite 3 valores para somar:");
    
    d= a + b - c;
    
    printf("A soma de a+b é %d",d);

    return 0;
}
